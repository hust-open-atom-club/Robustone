# Differential Tests

This directory tracks differential testing artifacts that compare Robustone
against Capstone.

Current source of truth:

- `python3 test/run_tests.py --all`
- `make test`

Known intentional differences belong in `known-differences.toml`.
